# ReadWritePaths= requires this source path before systemd builds the service
# mount namespace. tmpfiles creates it during boot; ExecStartPre re-attests the
# owner/group/mode on every service start.
d /run/openmaus-provider 2750 openmaus-server openmaus-runtime -
